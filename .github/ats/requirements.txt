# Core scoring & NLP (graceful fallbacks if missing)
numpy>=1.26.0
scikit-learn>=1.3.0
rapidfuzz>=3.5.2
textstat>=0.7.3
language-tool-python>=2.7.1
dateparser>=1.1.8

# Semantic models (optional but recommended)
sentence-transformers>=2.2.2
torch>=2.2.0

# Optional NER for company/title hints
spacy>=3.7.0
# (install the model once: python -m spacy download en_core_web_sm)

# PDF extraction & OCR
PyMuPDF>=1.24.0
pdfminer.six>=20221105
pdfplumber>=0.10.0
Pillow>=10.0.0
pytesseract>=0.3.10
